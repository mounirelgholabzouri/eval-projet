# Environment variables for docker-compose
# Copy this file to .env.local and customize for your environment

# Database Configuration
DB_NAME=eval_online
DB_USER=eval_user
DB_PASS=eval_password
DB_ROOT_PASS=root_password

# PHP Environment
PHP_ENV=development

# Ports (adjust if needed)
APP_PORT=8080
DB_PORT=3306
